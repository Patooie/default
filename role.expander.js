/*
 * This code stub is intended to be a starting point for the aggressive expander that will move to adjacent rooms, claim the controller, set up a spawn and block the exits
 * 
 *
 * 
 * var mod = require('role.expander');
 * mod.thing == 'a thing'; // true
 */

module.exports = {

};